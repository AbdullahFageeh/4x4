import { PaymentType } from '../../../payment_type';
import type { PaymentRequestSource } from '../payment_request_source';
export declare class SamsungPayRequestSource implements PaymentRequestSource {
    type: PaymentType;
    samsungPayToken: string;
    manualPayment: string;
    saveCard?: boolean;
    constructor({ samsungPayToken, manualPayment, saveCard, }: {
        samsungPayToken: string;
        manualPayment?: boolean;
        saveCard?: boolean;
    });
    toJson(): Record<string, any>;
}
//# sourceMappingURL=samsung_pay_request_source.d.ts.map