import { PaymentType } from '../../../payment_type';
import type { PaymentRequestSource } from '../payment_request_source';
export declare class ApplePayRequestSource implements PaymentRequestSource {
    type: PaymentType;
    applePayToken: string;
    manualPayment: string;
    saveCard?: boolean;
    constructor({ applePayToken, manualPayment, saveCard, }: {
        applePayToken: string;
        manualPayment?: boolean;
        saveCard?: boolean;
    });
    toJson(): Record<string, any>;
}
//# sourceMappingURL=apple_pay_request_source.d.ts.map