import { CreditCardNetwork } from '../../../credit_card_network';
import { PaymentType } from '../../../payment_type';
import type { PaymentRequestSource } from '../payment_request_source';
export declare class CreditCardRequestSource implements PaymentRequestSource {
    type: PaymentType;
    network: CreditCardNetwork;
    name: string;
    number: string;
    cvc: string;
    month: string;
    year: string;
    saveCard: string;
    manual: string;
    constructor({ name, number, cvc, month, year, tokenizeCard, manualPayment, }: {
        name: string;
        number: string;
        cvc: string;
        month: string;
        year: string;
        tokenizeCard?: boolean;
        manualPayment?: boolean;
    });
    toJson(): Record<string, any>;
}
//# sourceMappingURL=credit_card_request_source.d.ts.map