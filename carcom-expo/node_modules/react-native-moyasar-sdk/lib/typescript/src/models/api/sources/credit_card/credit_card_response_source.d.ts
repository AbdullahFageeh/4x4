import type { CreditCardNetwork } from '../../../credit_card_network';
import { PaymentType } from '../../../payment_type';
import type { PaymentResponseSource } from '../payment_response_source';
export declare class CreditCardResponseSource implements PaymentResponseSource {
    type: PaymentType;
    network: CreditCardNetwork;
    name: string;
    number: string;
    gatewayId: string;
    transactionUrl: string;
    referenceNumber?: string;
    token?: string;
    message?: string;
    constructor({ company, name, number, gatewayId, transactionUrl, referenceNumber, token, message, }: {
        company: CreditCardNetwork;
        name: string;
        number: string;
        gatewayId: string;
        transactionUrl: string;
        referenceNumber: string;
        token: string;
        message: string;
    });
    static fromJson(json: Record<string, any>): CreditCardResponseSource;
}
//# sourceMappingURL=credit_card_response_source.d.ts.map