import { PaymentSplit } from '../../payment_split';
import type { PaymentStatus } from '../../payment_status';
import { PaymentType } from '../../payment_type';
import type { PaymentResponseSource } from '../sources/payment_response_source';
/**
 Moyasar API response for processing a payment.
 */
export declare class PaymentResponse {
    id: string;
    status: PaymentStatus;
    amount: number;
    fee: number;
    currency: string;
    refunded: number;
    refundedAt?: string;
    captured: number;
    capturedAt?: string;
    voidedAt?: string;
    description?: string;
    amountFormat: string;
    feeFormat: string;
    refundedFormat: string;
    capturedFormat: string;
    invoiceId?: string;
    ip?: string;
    callbackUrl?: string;
    createdAt: string;
    updatedAt: string;
    metadata?: Record<string, string | number | boolean> | null;
    source: PaymentResponseSource;
    splits?: PaymentSplit[] | null;
    constructor({ id, status, amount, fee, currency, refunded, refundedAt, captured, capturedAt, voidedAt, description, amountFormat, feeFormat, refundedFormat, capturedFormat, invoiceId, ip, callbackUrl, createdAt, updatedAt, metadata, source, splits, }: {
        id: string;
        status: PaymentStatus;
        amount: number;
        fee: number;
        currency: string;
        refunded: number;
        refundedAt?: string;
        captured: number;
        capturedAt?: string;
        voidedAt?: string;
        description?: string;
        amountFormat: string;
        feeFormat: string;
        refundedFormat: string;
        capturedFormat: string;
        invoiceId?: string;
        ip?: string;
        callbackUrl?: string;
        createdAt: string;
        updatedAt: string;
        metadata?: Record<string, string | number | boolean> | null;
        source: PaymentResponseSource;
        splits?: PaymentSplit[] | null;
    });
    /**
     * Creates a new PaymentResponse instance from a JSON object.
     */
    static fromJson(json: Record<string, any>, paymentType: PaymentType): PaymentResponse;
}
//# sourceMappingURL=payment_response.d.ts.map