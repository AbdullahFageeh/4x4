export declare class CreditCardConfig {
    saveCard: boolean;
    manual: boolean;
    /**
     * @param {boolean} saveCard - An option to save (tokenize) the card after a successful payment.
     * @param {boolean} manual - An option to enable the manual auth and capture flow.
     */
    constructor({ saveCard, manual, }: {
        saveCard?: boolean;
        manual?: boolean;
    });
}
//# sourceMappingURL=credit_card_config.d.ts.map