"use strict";

import { getConfiguredLocalizations } from "../../localizations/i18n.js";
import { FieldValidator } from "./field_validator.js";
export class CreditCardNameValidator extends FieldValidator {
  latinRegex = /^[a-zA-Z\s-]+$/;
  constructor() {
    super();
    const {
      t
    } = getConfiguredLocalizations();
    this.addRule(t('moyasarTranslation:nameRequired'), value => {
      return value.length === 0;
    });
    this.addRule(t('moyasarTranslation:onlyEnglishAlphabets'), value => {
      return !this.latinRegex.test(value);
    });
    this.addRule(t('moyasarTranslation:bothNamesRequired'), value => {
      return value.split(' ').filter(name => name !== '').length < 2;
    });
  }
}
//# sourceMappingURL=credit_card_name_validator.js.map