"use strict";

import i18n from 'i18next';
import { FieldValidator } from "./field_validator.js";
export class PhoneNumberValidator extends FieldValidator {
  constructor() {
    super();
    this.addRule(i18n.t('moyasarTranslation:phoneNumberRequired'), value => {
      return value.length === 0;
    });
    this.addRule(i18n.t('moyasarTranslation:phoneNumberOnlyDigits'), value => {
      return !/^\d+$/.test(value);
    });
    this.addRule(i18n.t('moyasarTranslation:phoneNumberInvalid'), value => {
      return !/^05/.test(value);
    });
    this.addRule(i18n.t('moyasarTranslation:phoneNumberInvalidCount'), value => {
      return value.length !== 10;
    });
  }
}
//# sourceMappingURL=phone_number_validator.js.map