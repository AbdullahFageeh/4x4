"use strict";

import { PaymentType } from "../../../payment_type.js";
export class ApplePayPaymentResponseSource {
  type = PaymentType.applePay;
  constructor({
    number,
    gatewayId,
    referenceNumber,
    message,
    token
  }) {
    this.number = number;
    this.gatewayId = gatewayId;
    this.referenceNumber = referenceNumber;
    this.message = message;
    this.token = token;
  }
  static fromJson(json) {
    return new ApplePayPaymentResponseSource({
      number: json.number,
      gatewayId: json.gateway_id,
      referenceNumber: json.reference_number,
      message: json.message,
      token: json.token
    });
  }
}
//# sourceMappingURL=apple_pay_response_source.js.map