"use strict";

import { PaymentType } from "../../../payment_type.js";
export class SamsungPayPaymentResponseSource {
  type = PaymentType.samsungPay;
  constructor({
    number,
    gatewayId,
    referenceNumber,
    message,
    token
  }) {
    this.number = number;
    this.gatewayId = gatewayId;
    this.referenceNumber = referenceNumber;
    this.message = message;
    this.token = token;
  }
  static fromJson(json) {
    // TODO: Should we support more fields? Also for Apple Pay
    return new SamsungPayPaymentResponseSource({
      number: json.number,
      gatewayId: json.gateway_id,
      referenceNumber: json.reference_number,
      message: json.message,
      token: json.token
    });
  }
}
//# sourceMappingURL=samsung_pay_response_source.js.map