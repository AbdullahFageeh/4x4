"use strict";

import { PaymentType } from "../../../payment_type.js";
export class ApplePayRequestSource {
  type = PaymentType.applePay;
  constructor({
    applePayToken,
    manualPayment = false,
    saveCard = false
  }) {
    this.applePayToken = applePayToken;
    this.manualPayment = manualPayment ? 'true' : 'false';
    this.saveCard = saveCard;
  }
  toJson() {
    return {
      type: this.type,
      token: this.applePayToken,
      manual: this.manualPayment,
      save_card: this.saveCard
    };
  }
}
//# sourceMappingURL=apple_pay_request_source.js.map